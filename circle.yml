machine:
  ruby:
    version: 2.3.1
deployment:
  production:
    pre:
      - pyenv global 2.7.12
dependencies:
  pre:
    - gem install bundler
checkout:
  post:
    - bundle install
    - bash automated_build.sh